# Dataset configuration v3 — four sources (MatsyaDx-BD / Mendeley excluded)

**Dataset configuration v3 excludes MatsyaDx-BD/Mendeley.** Project decision of
2026-09-20, final. The `mendeley` key is removed from every manifest, split, fold,
GAN run and evaluation from this version on. Its definition is kept in
`src/multi_dataset.py::EXCLUDED_SOURCES` (never walked, linked, hashed or split) and
the complete v2 audit that included it is preserved, unchanged, under
`data/audit/archive/v2_mendeley/` (master dataset, inventory, label mapping,
duplicate / dedup / leakage reports, `split_v2/`, `cv_v2/`, CLAHE validation).

## Active sources (`src/multi_dataset.py::DATASET_SOURCES`, `DATASET_CONFIG_VERSION = "v3"`)

| key | delivered folder (Drive `MyDrive/AquaHealth/` and the local drop) | raw images |
|---|---|---|
| current_freshwater | `Fresh_water_disease/{train_split, test_split, test.csv}` | 3,503 |
| kaptai | `Fresh Water Fish Dataset/` | 133 |
| roboflow | `Fish Disease.v1i.folder/` | 454 |
| paper_dataset | `SalmonScan … Aquaculture System/SalmonScan/` | 1,208 |
| **total** | | **5,298** |

Everything below was generated by the existing scripts on 2026-09-20 with the
methodology unchanged (same canonical 8-class taxonomy, same `LABEL_MAPPING`, same
unresolved-label policy, same SHA-256 / dHash duplicate definitions, same group-aware
class|source-stratified split rule, seed 42). No count was edited by hand.

## Clean corpus (`scripts/build_clean_manifest.py` → `clean_manifest.csv`, `dedup_report.*`)

| quantity | v3 (four sources) | v2 (with Mendeley, archived) |
|---|---|---|
| raw images | 5,298 | 7,435 |
| **included (usable)** | **3,805** | 5,942 |
| excluded | 1,493 | 1,493 |
| — unresolved label | 1,277 (kaptai 69, paper_dataset 1,208) | 1,277 |
| — exact duplicate | 162 (current_freshwater 74, roboflow 88) | 162 |
| — label-conflict group | 47 (current_freshwater 28, roboflow 15, kaptai 4) | 47 |
| — excluded class | 7 (kaptai) | 7 |
| included per source | current_freshwater 3,401 · kaptai 53 · roboflow 351 · paper_dataset 0 | + mendeley 2,137 |
| exact duplicate groups | 171 (88 cross-dataset) | 171 |
| near-duplicate groups (distinct bytes) | 540 (211 cross-dataset) | 693 |
| label-conflict groups | 12 (48 images) | 12 |
| corrupt files | 0 | 0 |

The exclusion counts are identical to v2 because every Mendeley image was included in
v2 and no duplicate group involved it; removing the source removes 2,137 included images
and its 153 same-specimen/near groups, nothing else.

Included per canonical class (v3): Aeromoniasis 485 · Bacterial Gill Disease 493 ·
Bacterial Red Disease 453 · EUS Disease 459 · Healthy Fish 563 · Parasitic Disease 461 ·
Saprolegniasis 442 · White Tail Disease 449.

`paper_dataset` (SalmonScan) contributes 0 usable images: its two classes
(`FreshFish`, `InfectedFish`) remain `LABEL_UNRESOLVED` under the canonical mapping
(`'infected' names no disease`), exactly as in v2. It stays an active source so its
files are inventoried and its duplicate/leakage evidence is kept.

## Leakage audit (`scripts/audit_leakage.py` → `leakage_report.*`)

3,222 leakage groups over the 3,805 included images (485 multi-image groups holding
1,068 images; 180 groups span datasets; 200 span the vendors' own train/valid/test
folders; largest group 6; 0 included groups with mixed labels). Specimen ids: none in
the active sources (the only dataset that shipped them was MatsyaDx-BD). Resolution
shortcut upper bound 0.271 vs majority baseline 0.148 (unchanged finding: documented,
not silently fixed). EXIF orientation tags: 27 kaptai images (decision still open, as
in v2). Methodology unchanged.

## Frozen split v3 (`scripts/build_dev_test_split.py --test-ratio 0.20 --seed 42` → `split_v3/`)

| partition | images | groups |
|---|---|---|
| development | 3,044 | 2,585 |
| **final_test (FROZEN)** | **761** | 637 |

Per class test share 0.198–0.201; per source: current_freshwater 2,721/680,
kaptai 45/8, roboflow 278/73. Groups straddling partitions: 0.

`split_v3/split_manifest.sha256`:

```
68b5f8f7cd60f0fda6ecad2241f27a1fa2af4c0c35ceca9fcfc5ba4d940e6599  development.csv
baa330347a89305212b08ac4c79a1bb6a98cdd7447172f4aa3aea113c16cb7a2  final_test.csv
```

`final_test.csv` is opened once, at the final evaluation. Never for training, GAN
training or generation decisions, CV, tuning, architecture/model selection or
overfitting correction.

## 10-fold CV v3 (`scripts/build_cv_folds.py --folds 10 --seed 42` → `cv_v3/`)

Validation sizes 315, 311, 308, 308, 304, 304, 303, 299, 297, 295 (train 2,729–2,749);
every class and every source in every validation fold; groups shared with train: 0 in
all ten folds. Digests in `cv_v3/folds.sha256`.

## Downstream

`src/split_v2.py` (the split *policy* module, unchanged) now names `SPLIT_DIR_NAME =
"split_v3"` and `CV_DIR_NAME = "cv_v3"`; every consumer (Colab bundle, preflight, GAN,
CV runner) follows the constants. The old local MPS fold-1 GAN output (v2 data) is
quarantined under `data/gan_archive_unofficial/` and is not an experiment.

## GAN plan on v3 (computed by `plan_synthetic_counts` from the fold manifests, not assumed)

Per-fold planned synthetic images (class-aware: top-up to the fold's largest class, the
largest class — Healthy Fish in every fold — gets 0, cap 1× real): fold 1 = 495, 2 = 491,
3 = 504, 4 = 504, 5 = 500, 6 = 500, 7 = 507, 8 = 503, 9 = 501, 10 = 499 →
**5,004 planned over 10 folds** (v2's 23,616 no longer applies). **Generated on Colab CUDA
(Tesla T4, 2026-09-20): 495 / 491 / 504 / 504 / 500 / 500 / 507 / 503 / 501 / 499 = 5,004, failed 0;
`verify_gan_outputs.py --images all` 229/229 checks VERIFIED** (`results/v2/gan/`,
`docs/GAN_AUGMENTATION.md` §8). Fold-1 forbidden ids = 315 validation + 761 final test = 1,076.

## Reproducibility anchors

- clean manifest SHA-256 `e90b43a0c5a5cf9ea61ec3140c048d1e8ef854874492eaeafaeed04b56c86b68`
- `configs/preprocess_v2_clahe.json` SHA-256 `4c35039a73c0d8740c736de00339c240d405849b885b0231d4688a3508499ab7`
  (CLAHE LAB-L clip 2.0 tile 8×8 → Resize 256 → CenterCrop 224 → ImageNet normalisation)
- seed 42 everywhere (split, folds, GAN, experiments)
