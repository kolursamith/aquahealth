# data/combined_v2/ — reserved

The combined dataset of the new experiment is represented by the manifest
`data/audit/master_dataset.csv` (provenance, label mapping, hashes, duplicate
groups per image), not by copied files. Physically reorganising images into
unified class folders is deferred until the label mapping
(`data/audit/label_mapping.csv`) has been reviewed and the UNRESOLVED rows
decided. No file lives here yet; images are git-ignored in any case.
