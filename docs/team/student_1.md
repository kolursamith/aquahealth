# Student 1 — Core AI

## Owns

- `src/model.py` — EfficientNet-B0 model definition
- `src/train.py` — training loop, checkpointing
- `src/predict.py` — jointly with Student 2

## Branch

`student-1`, merged into `develop` via pull request.

## Getting started

```bash
git checkout develop
git checkout student-1
pip install -r requirements-dev.txt
python scripts/verify_environment.py
```
