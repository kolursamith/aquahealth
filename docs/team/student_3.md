# Student 3 — Frontend

## Owns

- `app/components/upload.py`
- `app/components/result_card.py`
- `app/components/risk_card.py`

## Branch

`student-3`, merged into `develop` via pull request.

## Getting started

```bash
git checkout develop
git checkout student-3
pip install -r requirements/dev.txt
streamlit run app/app.py
```

Uses `app/mock_prediction.py` during development, so no trained model is
required.
